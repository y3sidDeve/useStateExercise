// Title.js

import React from 'react';

// Definir el componente Title
function Title(props) {
  return <h1 className='text-center col-lg-12 fw-bold'>{props.text}</h1>;
}

// Exportar el componente Title
export default Title;
